#!/usr/bin/env python3
"""深度诊断：对比规划分组 vs 实际运动，追踪前进方向和稳定性。"""

import json
from pathlib import Path
from typing import Dict, List

import numpy as np


def analyze_issues():
    """分析三个问题的根本原因。"""
    print("=" * 90)
    print("深度诊断：分组执行不匹配、前进方向偏差、动态稳定性问题")
    print("=" * 90)
    
    # 问题 1: 分组不匹配的可能原因
    print("\n[问题 1] 实际运动未按分组执行")
    print("-" * 90)
    print("""
可能原因：

1.1 smoothstep 过渡带太宽 (edge±0.30)
    - 导致相邻腿的摆动重叠，多条腿同时离地，破坏分组结构
    - 应该改小 edge 值或改用阶梯函数

1.2 相位偏置不够明显
    - 虽然分组生成了 π 相位偏置，但在 build_gait_targets() 中
      所有腿共用同一个全局 phase = 2π*freq*sim_time
    - 即使分组内部有 π 差，由于共用全局时间，相位偏置可能被全局相位掩盖

1.3 PID 控制跟踪不足
    - 底层 PID 位置控制的 stiffness/damping 可能不够强
    - 导致关节无法精确跟踪目标，多条腿发生"滑动"

1.4 缺少实时足端接触反馈
    - 目前没有检测腿是否真的接触地面
    - 规划说"腿应该支撑"，但腿可能已经滑出或陷入
    """)
    
    # 问题 2: 前进方向和运动轨迹
    print("\n[问题 2] 机器人未沿规划前进方向匀速直线运动")
    print("-" * 90)
    print("""
可能原因：

2.1 虚拟弹簧阻尼导致的横向瞬间力
    - 当一侧分组摆腿时，足端侧向力不对称
    - 缺乏足端力/接触事件的反馈，无法主动补偿

2.2 足端滑动 (slip)
    - 地面摩擦力可能不足（当前配置：static_friction=1.8）
    - 或者足端着陆位置相对于推进力方向有偏差

2.3 相位不同步导致的扭矩偏差
    - 如果分组腿的相位差不足 π（例如只有 0.9π）
    - 会导致一侧主动，一侧部分摆动，产生偏航力矩

2.4 前进方向轴线本身可能不稳定
    - PCA 提取的 final_forward_axis 基于足端位置，
      但在运动中足端位置变化，轴线也会漂移
    """)
    
    # 问题 3: 动态稳定性
    print("\n[问题 3] 动态稳定性差")
    print("-" * 90)
    print("""
可能原因：

3.1 支撑相腿数不足或不稳定
    - 对角线三足设计：任意时刻 3 条腿支撑，3 条腿摆动
    - 但当摆腿刚要着地、支撑腿正要抬起时，过渡时刻支撑不足
    - 导致质心投影落在支撑域边界附近或外侧 → SSM < 0

3.2 虚拟阻尼不足，机身晃动
    - 当腿交替时，质心没有充分的虚拟弹簧约束
    - 质心可能垂直振荡或侧向摇摆

3.3 PID 增益配置不适配动态步态
    - 当前配置针对"站立"位置控制优化
    - 在快速摆腿时，stiffness/damping 比例可能导致超调或滞后

3.4 缺少足端主动阻抗控制
    - 当足端踩到斜面或软地时，应该自动调节 stiffness
    - 目前只有硬编码的固定增益，无法适应地形

3.5 相位摆不够光滑
    - smoothstep 的 edge±0.30 导致关节目标在摆/支间切换时不连续
    - 产生脉冲力，加剧不稳定
    """)
    
    # 诊断建议
    print("\n[诊断建议] 优先级")
    print("-" * 90)
    print("""
优先级 1（紧急修复）：
  ✓ 缩小 smoothstep 的过渡宽度 (edge±0.10 或改用更平滑函数)
  ✓ 打印每帧实际腿部状态 vs 目标状态 (在可视化循环中输出)
  ✓ 监测质心投影相对支撑域的位置 (SSM 值)

优先级 2（稳定性改进）：
  ✓ 增加 PID 增益 (特别是 drop 关节，当前 stiffness=250 可能不够)
  ✓ 改进前进方向估计 (考虑动态中的足端位置更新，而非固定 PCA 轴)
  ✓ 加入脚底接触检测 (获取 net_contact_force 反馈)

优先级 3（CPG + 主动阻抗）：
  ✓ 集成完整 CPG 网络 (现在只有分组信息，没有耦合振荡)
  ✓ 实现虚拟阻抗控制 (足端侧向力 → 动态阻尼调节)
    """)
    
    # 实施检查清单
    print("\n[实施检查清单] 立即可做的诊断")
    print("-" * 90)
    print("""
[ ] 1. 在可视化中添加每帧输出：
        - 规划的腿部相位 vs 实际关节位置
        - 每条腿的 sin(phase) 与 swing_alpha 平滑值
        - 质心 SSM 值（支撑域内/外）

[ ] 2. 记录机器人运动轨迹中的偏差：
        - 起始方向 vs 实际运动方向的夹角
        - 侧向漂移距离

[ ] 3. 测试缩小 smoothstep 边界：
        改 edge±0.30 → edge±0.10，观察是否减少多腿同时摆动

[ ] 4. 检查足端接触力平衡：
        在仿真中读取 gym.get_net_contact_force_tensor()，
        验证 3 条支撑腿是否均等分担重量
    """)


if __name__ == "__main__":
    analyze_issues()
